apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-nginx-controller
  namespace: ingress-nginx
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/instance: ingress-nginx
    app.kubernetes.io/name: ingress-nginx
    app.kubernetes.io/part-of: ingress-nginx
data:
  # Enable snippet annotations for advanced authentication and routing
  # Required for user-specific authentication and token handling
  allow-snippet-annotations: "true"

  # Optional: Add other custom NGINX configurations here
  # proxy-body-size: "50m"
  # proxy-connect-timeout: "3600"
