# ============================================================================
# Tesslate Studio Kubernetes Deployment Configuration
# ============================================================================
# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update DOCR_TOKEN below
# 3. Create secrets: Copy manifests/security/app-secrets.yaml.example
# 4. Run: ./scripts/deployment/deploy-all.sh
# 5. Never commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Container Registry Authentication (REQUIRED)
# ----------------------------------------------------------------------------
# DigitalOcean Container Registry Token
# Used by: setup-registry-auth.sh, build-push-images.sh, deploy-all.sh
# Get your token from: https://cloud.digitalocean.com/account/api/tokens
DOCR_TOKEN=YOUR_DOCR_TOKEN_HERE

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# - Application secrets are configured in manifests/security/app-secrets.yaml
# - Database credentials are in manifests/database/postgres-secret.yaml
# - DEPLOYMENT_MODE and namespace configs are set in K8s manifests
# - Domain and SSL configs are in manifests/core/main-ingress.yaml
