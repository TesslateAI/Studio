# ============================================================================
# Tesslate Studio Backend Configuration
# ============================================================================
# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Update the values below for your environment
# 3. Never commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Security & Authentication (REQUIRED)
# ----------------------------------------------------------------------------
SECRET_KEY=your-secret-key-here-change-this-in-production

# ----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Development (SQLite - default):
DATABASE_URL=sqlite+aiosqlite:///./builder.db

# Production (PostgreSQL):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/tesslate

# ----------------------------------------------------------------------------
# Deployment Mode (REQUIRED)
# ----------------------------------------------------------------------------
# Controls which container management system is used:
# - "docker"      : Docker containers with Traefik routing (local development)
# - "kubernetes"  : Kubernetes Deployments with Ingress (production)
DEPLOYMENT_MODE=docker

# ----------------------------------------------------------------------------
# AI Configuration (REQUIRED)
# ----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# ----------------------------------------------------------------------------
# Optional Configuration
# ----------------------------------------------------------------------------
# These have defaults in config.py - only override if needed

# JWT Algorithm (default: HS256)
# ALGORITHM=HS256

# Token expiration (defaults: 30 minutes, 14 days)
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# REFRESH_TOKEN_EXPIRE_DAYS=14

# Base URL for dev containers (mainly for K8s mode)
# Docker mode: Leave empty (uses .localhost domains)
# Kubernetes mode: Set to your ingress domain
# DEV_SERVER_BASE_URL=https://studio-test.tesslate.com

# ----------------------------------------------------------------------------
# GitHub OAuth Configuration (OPTIONAL but required for GitHub features)
# ----------------------------------------------------------------------------
# Create a GitHub OAuth App: https://github.com/settings/applications/new
# - Application name: Tesslate Studio (or your app name)
# - Homepage URL: http://localhost:5173 (or your production URL)
# - Authorization callback URL: http://localhost:5173/auth/github/callback
#
# For production, use your domain:
# - Homepage URL: https://your-domain.com
# - Callback URL: https://your-domain.com/auth/github/callback
#
# GITHUB_CLIENT_ID=your_github_oauth_app_client_id
# GITHUB_CLIENT_SECRET=your_github_oauth_app_client_secret
# GITHUB_OAUTH_REDIRECT_URI=http://localhost:5173/auth/github/callback

# ----------------------------------------------------------------------------
# CORS and Security Configuration (OPTIONAL)
# ----------------------------------------------------------------------------
# CORS Allowed Origins (comma-separated list)
# Add all frontend URLs that should be allowed to access the API
# For local development, include localhost ports
# For production, include your production domain(s)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000,https://studio-test.tesslate.com,https://studio-demo.tesslate.com

# Allowed Hosts (comma-separated list)
# List of hostnames allowed for Vite dev server and Content Security Policy
# Use *.localhost for wildcard local subdomain support
# ALLOWED_HOSTS=your-domain.com,studio-test.tesslate.com,studio-demo.tesslate.com,localhost,*.localhost