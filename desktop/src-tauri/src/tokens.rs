// Stronghold-backed token storage — placeholder.
//
// The sidecar currently persists paired cloud credentials at
// `$TESSLATE_STUDIO_HOME/cache/cloud_token.json` via
// `orchestrator/app/services/token_store.py`. Moving that blob into a
// Stronghold vault managed by the Rust host is a follow-up.
